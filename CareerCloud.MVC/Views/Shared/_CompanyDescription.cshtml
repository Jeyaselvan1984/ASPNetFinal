@model IEnumerable<CareerCloud.Pocos.CompanyDescriptionPoco>

 <h3> List of Companies</h3>

<table class="table">
    <thead>
        <tr>
           <th>
                @Html.DisplayNameFor(model => model.CompanyName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanyDescription)
            </th>
            <th>
                Job List
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyDescription)
            </td>
            <td>
                <a asp-action="DisplayJobsForGivenCompany" asp-controller="CompanyJob" asp-route-CompanyId="@item.Company">Jobs</a>
            </td>
        </tr>
}
    </tbody>
</table>
